1) Создать базу данных в MySQL
2) Открываем проект (он будет залит на гитхаб). Вводим composer install. Данные по БД нужно будет перенести в файл .env
3) в терминале вводим поочередно:
    php artisan migrate
    php artisan serve
Файл swagger.json располагается в корне проекта
